<%- include ./partials/header.ejs %>
<% function msToHMS( duration ) {

    var millisecondes = parseInt((duration % 1000) / 100),
       secondes = parseInt((duration / 1000) % 60),
       minutes = parseInt((duration / (1000 * 60)) % 60),
       heures = parseInt((duration / (1000 * 60 * 60)) % 24);

     heures = (heures < 10) ? "0" + heures : heures;
     minutes = (minutes < 10) ? "0" + minutes : minutes;
     secondes = (secondes < 10) ? "0" + secondes : secondes;

     return heures + ":" + minutes + ":" + secondes ;
}

%> 
  
    <% if (Date.now() - currentUser.createdAt < 1000) { %>
        <h1>Bienvenue nouvel usager</h1>     
    <% } else { %>
        <h1>Login réussi! Nombre d'usagers : <%=  data.user_count %>  </h1>     
    <% } %>


    
    <% if (data.event_owner.length > 0) { %>
        <h1>Évènement créés : </h1>        
    <% } %>
    <ul>
        <% data.event_owner.forEach(element => { %>
         <li>  <a href="/join_event/<%= element._id %>"><%= element.nom + " - " + element.description + " "%></a>  
            <% data.event_count_user.forEach(count => { %>                
                <% if (count._id.toString() === element._id.toString()) { %>
                    == Nombre d'usagers inscrits : <%= count.total %> 
                <% } %>
                
            <% }) %>
            <button type="button" class="btn btn-danger"> <a href="/generate_schedule/<%= element._id %>">Générer horaire</a></a></button></li>
        <% }) %>
    </ul>

    <% if (data.dispo_event.length > 0) { %>
        <h1>Évènement joints : </h1>        
    <% } %>
    <ul>
        <% data.dispo_event.forEach(element => { %>
         <li>  <a href="/join_event/<%= element.event._id %>"><%= element.event.nom + " - " + element.event.description %></a></li>
        <% }) %>
    </ul>
    


<button type="button" class="btn btn-danger"> <a href="/create_event">Créer un évènement</a></a></button>



<form  id="joineventform" class="formulaire register-form" action="" autocomplete="off">
<label  for="event_id">Veuillez fournir l'ID de l'évènement:</label>
<input  type="text" name="event_id" placeholder="ID de l'évènement" value="6057a483396dbc1eb0f020d6">
<button type="submit" class="btn btn-danger" >Joindre évènement </button>

</form>


<%- include ./partials/footer.ejs %>
